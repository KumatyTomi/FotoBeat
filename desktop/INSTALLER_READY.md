# Installer ready path

Workflow: `.github/workflows/windows-installer.yml`.

Artifact: `FotoBeat-Windows-Installer`.

Output directory inside build: `desktop/release`.

Release tags: pushing `v*` publishes the Windows installer files to the matching GitHub Release.
